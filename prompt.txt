I'm designing a deterministic statistical significance step (Z-score style anomaly flagging) that will run in Databricks before data reaches our LLM pipeline. I need precise facts from the codebase, not summaries.

Examine these files:
1. packages/store_data_tap/src/.../merch/queries.py
2. packages/store_data_tap/src/.../merch/model.py
3. packages/store_data_tap/src/.../merch/aggregate.py
4. packages/store_data_tap/src/.../fresh_ops/queries.py (briefly - just note where it differs from merch)
5. packages/briefing_research/.../context/vertex.py and context/research.py
6. packages/store_data_tap/src/.../groundtruth.py

For each, report:
- Key functions with one-line purpose each
- EXACT metric/column names (sales, units, WTD comparisons, like-store fields, etc.)
- Date ranges queried: how many days/weeks of history do the Databricks queries actually pull? Quote the date filter logic verbatim
- Grain of the data: is it per store per department per day? Per week? Per category?
- The exact output schema (dataclass/pydantic/dict fields) of the anomaly or metric records in model.py
- In aggregate.py: the transformation sequence from raw query rows to final records
- In vertex.py/research.py: how ground-truth data is inserted into the LLM prompt - is it rendered as text, JSON, markdown tables? Show a small sample of the format
- In groundtruth.py: the structure of the written ground-truth documents

Also answer:
- Is there any existing statistics, thresholding, or filtering logic anywhere (even simple percent-change cutoffs)? Quote it
- Does anything already compute averages over historical windows?
- Where does like-store comparison data enter, and at what grain?

Output as a structured markdown doc I can copy elsewhere. Include short verbatim code snippets for schemas, date filters, and any threshold logic. Do not modify any files.


Follow-up fact-finding. Same rules: precise facts, verbatim snippets, no
summaries, do not modify files. Repo root: store-leader-assist/packages/
(currently contains briefing_research and store_data_tap).

MISSION 0 — does the new staged pipeline exist here?
Internal docs describe a newer package "briefing_pipeline" (ticket SAAI-362)
with concepts: EvidenceSet, numeric corpus, observe/hydrate/cull stages,
deterministic detectors, verifiers. Search this repo thoroughly for it:
- grep for: EvidenceSet, numeric corpus, hydrate, cull, observe, detector,
  ModelRetry, briefing_pipeline, SAAI-362
- check all branches if possible (git branch -a), and any sibling folders
If found: report its location and directory tree (2 levels).
If NOT found: say so explicitly - "briefing_pipeline not in this repo" -
and skip Mission 1.


MISSION 1 — hunt for daily-grain sales data (do this regardless)
Search the ENTIRE repo - both packages, all SQL strings, tests, configs,
AGENTS.md, READMEs, .gitlab-ci.yml, Dockerfile, any notebooks:
1. Grep for date-like columns in SQL: ID_DATE, REPT_DT, SALES_DT, DT_,
   CAL_DT, _daily, ORD_DT. List EVERY Databricks table name mentioned
   anywhere in the repo, with schema prefix (prod.dsr / prod.serve / other)
   and what grain the surrounding SQL implies.
2. For the dsr_hist_sales_*_dashboard_duration tables: any hint (comments,
   docstrings, column names) of an underlying daily-grain table they
   aggregate from?
3. Any code that BUILDS those dashboard tables (dbt, notebooks, SQL files)?
4. In briefing_research specifically, check: database.py, models.py,
   ground_truth.py, cron.py, researchers/sales_merchandising.py,
   prompts/persona.py - for MISSION 2 purposes report any SQL, plus:
   quote verbatim any prompt text in persona.py that instructs the LLM
   about significance, importance, prioritization, or comparing numbers.
5. departments.json in briefing_research/data: list its structure briefly.

Report as structured markdown. For anything not found, write "not found in
repo" - never guess.



