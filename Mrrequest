Hi Bob, sharing a quick status on the significance task:

- Quick win is implemented - since daily history isn't available yet, I built the peer-comparison version: each recommendation's department gets checked against how all other stores moved for the same window, and ones that are just following the market get sorted lower. Hooks in after generation, before DB save, nothing else touched. Testing locally right now.
- Long term - starting to hunt for the daily-grain sales table (our current tables are all pre-rolled into yesterday/WTD buckets, so no 30-day baseline is possible from them). Will check Confluence and the data platform team. Math side is already done, so it plugs in the moment we find the table.
- Design doc is ready, will go up as a draft MR once my GitLab access lands (Dominic's on it).
- Would also love 15 min with you at some point to match field names with your pipeline's evidence model, so this drops in cleanly when your branch merges.

Questions for you when you get a chance: peer set - all stores or region-level? And failed recommendations - sink quietly or show a "routine variance" tag?